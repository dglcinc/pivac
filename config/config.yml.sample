# The name of each section is the name of the module it configures.
# if a section is absent (commented out), or enabled is present and set to false, the module will not be used.

# this module enables input to the designated GPIO pin(s). Pin status is read in GPIO.status() function.
pivac.GPIO:
    description: Reports current high/low status of specified input pin(s)
    # options are "bcm" (GPIO numbers) or "board" (physical pin numbers); if absent, default is bcm
    numbering: "bcm"

    # used to temporarily disable by setting false; if absent, enabled = true
    enabled: true

    # names the path for the sk JSON output
    # puts both a .state boolean and .statenum int at this path: path.{outname}.{type}
    # sk_literal indicates whether outname will be part of the path
    sk_path: "electrical.ac.switch.utility"
    sk_literal: false

    # "pullup|pulldown" for internal resistors
    pullmode: "pullup"

    # propagated values can be specified once at top level, for all input keys, or once at top level with key-level exceptions
    propagate:
        - sk_path
        - pullmode
        - sk_literal

    # format is: pinnum: 
    #              outname : myname
    #              [sk_path: sk_pathstring]
    #              [pullmode: {pullup|pulldown}]
    inputs:
        17:
            outname: ZV
        27:
            outname: DHW
        22:
            outname: BLR
        5:
            outname: BOS2
        6:
            outname: BOS1
        12:
            outname: DEHUM
        # Was SCALA (booster-pump leak pan) until 2026-08-11; this input now
        # senses the Chiltrix chiller call. Rename in place, no wire moved.
        25:
            outname: CHIL
    # YOFF is RETIRED (2026-08-11) — not pending. There is no seasonal cutoff in
    # the CDP any more; winter shutdown is a manual breaker-off at the chiller.
    # Do NOT re-enable this or move it to BCM 19: the signal is gone, not homeless.
#        26:
#            outname: YOFF


pivac.OneWireTherm:
    description: Reports the current values of all temperature sensors on the 1-wire bus
    enabled: true

    # options for mode are: "fahrenheit", "kelvin", or "celsius"
    scale: "fahrenheit"

    # value is number of desired digits right of decimal for a temp value; 0 gets you an int; -1 gets no rounding
    #
    # KEEP THIS >= 2. Signal K output is always read in KELVIN (OneWireTherm.py), so
    # `rounding: 0` quantises every temperature to a whole Kelvin = 1.8 F. That is coarser
    # than the DS18B20's own 0.0625 C resolution and it destroys any delta-T built from two
    # sensors: a 10 F loop delta carries ~18% error, and the ~4 F primary delta across the
    # closely spaced tees is unusable. 2 decimals in Kelvin (0.018 F) captures full sensor
    # resolution. See docs/unico-air-handler-btu-monitoring-plan.md 2.6.
    rounding: 2

    sk_path: environment.inside.hvac
    sk_literal: false

    propagate:
        - sk_path
        - sk_literal
        - rounding
        - scale

    # this is list of known sensors by ID, and the name mapping. If no mapping here, result will be named with the sensor ID
    # sk_path, rounding, and scale be specified at sensor level. If not specified, top level value used.
    #
    # `offset` is a per-sensor calibration correction added to the reading, in KELVIN
    # (identical to degrees Celsius as a difference). The bench ice-point values below come
    # from docs/ds18b20-PA1-5-calibration.md. Apply them: the secondary-loop measurement is a
    # difference of a few degrees, and the probe-to-probe spread is larger than that.
    #
    # There is no physical outdoor DS18B20 — outdoor air comes solely from pivac.RedLink
    # (environment.outside.thermostat.temperature).
    inputs:
        # Primary loop, bracketing the closely spaced tees. IN - OUT is whole-system capacity.
        # Probes PA4A / PA4B, installed 2026-08-23.
        000000c98b14:
            outname: IN
            offset: 0.468
        0000001aa0a8:
            outname: OUT
            offset: 0.143
        # Buffer tank, upper and lower. Probes PA3A / PA3B, installed 2026-08-22.
        000000c9e879:
            outname: UBT
            offset: 0.348
        000000c915c1:
            outname: LBT
            offset: 0.466
        # Secondary loop A — kids room, then master bedroom (attic coil). Probes PA2A / PA2B.
        0000001ac0a9:
            outname: LOOPA_SUP
            offset: -0.184
        0000001a9154:
            outname: LOOPA_RET
            offset: 0.083
        # Secondary loop B — lower family room, kitchen, great room. Probes PA1A / PA1B.
        # Note the probe pair number and the loop letter differ: pair 1 serves loop B.
        000000cc0c90:
            outname: LOOPB_SUP
            offset: 0.588
        000000c99b56:
            outname: LOOPB_RET
            offset: 0.423
        0316a015e7ff:
            outname: Unassigned

pivac.LoopDelta:
    description: Per-run supply/return delta-T for the primary and secondary chilled-water loops
    enabled: true

    # Derived module: reads what pivac.OneWireTherm, pivac.GPIO and pivac.RedLink have
    # already published rather than touching hardware, so it does not contend for the
    # 1-wire bus or the GPIO pins and does not open a second Honeywell session.
    sk_url: http://localhost:3000/signalk/v1/api/vessels/self
    sk_path: environment.inside.hvac
    request_timeout: 5

    # Ignore any source value older than this. Stale is treated as missing, so a probe
    # that stopped reporting cannot keep feeding a delta-T.
    max_age_s: 120

    # Discard the first N seconds of every run. Cold water reaches the supply probe
    # before the return responds, so delta-T overshoots (+6.2 K measured at one minute
    # against a settled +4.0) and only lands by minute three.
    settle_s: 180

    daemon_sleep: 20

    # Each loop publishes <sk_path>.<name>.flowing every cycle, and
    # <sk_path>.<name>.deltaT -- the run's mean so far -- every cycle while that loop
    # is pumping and past the transient, and not at all otherwise. So each run draws
    # as its own segment: the value is readable and so is the call's duration, and the
    # segment's last sample is the full-run mean. KELVIN (a difference, so Fahrenheit
    # is * 9/5 with NO offset).
    #
    # `relay` is the switch that proves this loop is pumping. CHIL is the Taco, which
    # the HZ432 energizes on a call from ANY chiller zone -- including one on the other
    # loop -- so a secondary also needs `zones` to narrow it. Once each secondary pump
    # has its own sense input, set `relay` to that channel and delete `zones`: the relay
    # is read locally every 0.5 s while the zone state comes from Honeywell's cloud with
    # a measured median lag of 48 s.
    loops:
        primary:
            supply: IN
            return: OUT
            relay: CHIL
        LOOPA:
            supply: LOOPA_SUP
            return: LOOPA_RET
            relay: CHIL
            zones: [KIDS_ROOM, MASTER_BR]
        LOOPB:
            supply: LOOPB_SUP
            return: LOOPB_RET
            relay: CHIL
            zones: [DSTRS_FAM_ROOM]

pivac.TED5000:
    description: Reports real-time KWh usage from TED5000 MTUs (from LiveData.xml)
    enabled: true
    ipaddr: 192.168.1.124
    sk_path: electrical.ac.ted5000

    # takes the value at the specified path in LiveData.xml and returns it as the value for the specified name
    # sk_path can be specified at input level
    propagate:
        - sk_path
    inputs:
        Power/MTU1/PowerNow:
            outname: MTU1
        Power/MTU2/PowerNow:
            outname: MTU2
        Power/MTU3/PowerNow:
            outname: MTU3

pivac.RedLink:
    description: Reports thermostat info from mytotalconnectcomfort.com
    enabled: true

    # how long to sleep in sec between status calls. Default (if absent) is 0.5 sec.
    # 5 keeps WilhelmSK widget gaps short — a healthy parallel-refresh cycle is
    # 5–12s, so total period (cycle + sleep) lands around 10–17s.
    daemon_sleep: 5

    # socket timeout in seconds for the aiosomecomfort client (governs the
    # login flow on cold start). Cold-start login on the Pi takes ~75s due
    # to Honeywell's redirect-heavy login + TLS handshakes, so keep this
    # generous. Per-device refresh latency is bounded separately by
    # REFRESH_DEADLINE (12s) inside RedLink.py.
    request_timeout: 30

    # website
    site: "https://www.mytotalconnectcomfort.com/portal"
    
    # determines whether thermostat detail is browsed and scraped
    verbose: false

    # credentials
    uid: xxx
    pwd: xxx

    # no way to get the scale on the honeywell site; this scale is used for stats
    # override stats by name in inputs
    scale: fahrenheit
    propagate:
        - scale
    inputs:
        thermostat:
            sk_path: environment.inside.thermostat
        outdoor_sensor:
            sk_path: environment.outside.thermostat
# Framework settings — not a module. Holds non-module configuration such as
# the SignalK WebSocket connection. Keys here will not be treated as provider modules.
pivac_config:
    # SignalK WebSocket connection settings.
    # pivac-provider.py uses these to push delta messages directly to SignalK via WebSocket.
    # Create an admin user in SignalK if needed.
    signalk:
        host: localhost
        port: 3000
        username: admin
        password: xxx

pivac.ArduinoPSI:
    # NOTE: despite the legacy name, this section is the DHW board (10.0.0.114).
    # The Arduino module/delta names are inverted vs physical role — see CLAUDE.md.
    description: DHW pressure + hot-water recirc-loop temperature from one Arduino HTTP sensor
    # 'module' tells the framework which Python module to load for this config section.
    # Multiple config sections can share one implementation this way.
    module: pivac.ArduinoSensor
    enabled: true
    ipaddr: 10.0.0.114
    # Decimal places kept on temperature inputs, following the OneWireTherm
    # convention: 0 rounds to whole Kelvin, a positive value keeps that many
    # decimals, a negative value leaves the reading alone. Defaults to 2.
    # Settable per-input as well; readable here without listing it in
    # `propagate`. Temperatures are stored in KELVIN, so whole Kelvin is
    # 1.8 degF -- coarser than the sensor and fatal to any delta built from
    # two readings. Leave this at 2 unless you have a reason not to.
    rounding: 2
    inputs:
        # Each input key is the field name in the Arduino's JSON response.
        psi:
            sk_path: propulsion.mainEngine.coolant
            outname: pressure
        # A DS18B20 on the same Arduino reports the DHW recirc-loop temperature.
        # type: temperature converts the raw reading (scale, default fahrenheit) to
        # Kelvin and emits at {sk_path}.{outname}.temperature, e.g.
        # environment.inside.hvac.dhw.recirc.temperature
        temp:
            sk_path: environment.inside.hvac.dhw
            outname: recirc
            type: temperature
            scale: fahrenheit

pivac.ArduinoThermPSI:
    # NOTE: despite the legacy name, this section is the BOILER/hydronic board
    # (10.0.0.219) — the names are inverted vs physical role, see CLAUDE.md.
    description: Boiler/hydronic pressure from an Arduino HTTP sensor
    module: pivac.ArduinoSensor
    enabled: true
    ipaddr: 10.0.0.219
    inputs:
        psi:
            sk_path: propulsion.mainEngine.fuel
            outname: pressure

# ---------------------------------------------------------------------------
# Template for a new module — copy and adapt this block.
# The key (pivac.MyModule) must match your Python file name (pivac/MyModule.py),
# or set `module:` to point to a shared implementation.
# ---------------------------------------------------------------------------
# pivac.MyModule:
#     description: One-line description of what this module does
#     enabled: true
#
#     # Optional: seconds to sleep between polls in daemon mode (default 0.5)
#     daemon_sleep: 5
#
#     # Optional: override the Python module to load (for multi-instance sharing)
#     # module: pivac.SharedImplementation
#
#     # Optional: top-level config keys to copy down into each input entry
#     propagate:
#         - sk_path
#         - scale
#
#     # Signal K path prefix for this module's output values
#     sk_path: environment.inside.mymodule
#     sk_literal: false   # true = sk_path is the full path; false = path.outname.measurement
#
#     # For modules that poll a remote device:
#     # ipaddr: 10.0.0.x
#
#     # Named sensor inputs — each becomes a separate Signal K value
#     inputs:
#         my_sensor:
#             outname: reading_name   # used in Signal K path and default JSON output
#             sk_path: environment.inside.mymodule   # override top-level sk_path if needed

pivac.Emporia:
    description: Reports power consumption from Emporia Vue energy monitors
    enabled: true

    # How long to sleep between polls in daemon mode. Emporia data at 1-minute
    # scale doesn't update faster than once per minute, so 60s is appropriate.
    daemon_sleep: 60

    # Emporia account credentials. Token is cached at token_file so the
    # password is only used on first run or when the cached token expires.
    username: user@example.com
    password: xxx
    token_file: /etc/pivac/emporia-tokens.json

    # Signal K base path. Each panel and circuit appends to this:
    #   electrical.emporia.<panel_name>.<circuit_name>
    sk_path: electrical.emporia

    # Map device GIDs (from scripts/emporia-discover.py) to panel names.
    # Panel names become part of the Signal K path.
    panels:
        "123456789": house        # 200A main panel
        "987654321": apartment    # 100A apartment panel

pivac.FlirFX:
    description: Reports temperature and humidity data from a FLIRfx camera
    enabled: false
    daemon_sleep: 5

    inputs:
        # IP address of camera
        192.168.2.79:
            # if fake is true, insert dummy values instead of calling camera
            fake: true

            # put values here
            sk_path: environment.inside.temperature

            # change to your password
            pwd: 'xxxxxxx'

            # options for mode are: "fahrenheit", "kelvin", or "celsius"
            scale: "fahrenheit"
